name = "bobzhang/tar"

version = "0.1.1"

readme = "README.md"

repository = "https://github.com/moonbit-community/tar"

license = "ISC"

keywords = [ "tar", "archive", "compression", "streaming" ]

description = "Pure MoonBit library to read and write tar files with streaming support"

options(
  "alert-list": "+test_import_all",
)